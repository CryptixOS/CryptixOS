[default]
extend-ignore-identifiers-re = [
    # *sigh* this just isn't worth the cost of fixing
    "AttributeID.*Supress.*",
    "IST", "ist", "WHT", "unexpect",
    "implict", "ba", "EXTA",
    "Paeth", "isue", "childs",
    "fillin", "ful", "Nd", "onl",
    "RELA", "THM", "Rela", "INEX",
]

[files]
extend-exclude = [
    "patches/*", "Prism/*",
    "Meta/*.patch",
]

[default.extend-identifiers]
# *sigh* this just isn't worth the cost of fixing
AttributeIDSupressMenu = "AttributeIDSupressMenu"

[default.extend-words]
